# Public site origin (used in links, Open Graph, etc.)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database connection.
# Use ONE of these, depending on your setup.
# DATABASE_URL=postgres://user:pass@host:5432/dbname
# Or a local file path if you use SQLite:
# DB_PATH=./data/cure8.db

# Production by Vercel automatically sets NODE_ENV=production,
# but keep for completeness in non-Vercel deploys.
NODE_ENV=production
