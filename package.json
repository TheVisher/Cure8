{
  "name": "cure8",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@prisma/client": "^5.21.1",
    "next": "^15.1.4",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "server-only": "^0.0.1"
  },
  "scripts": {
    "dev": "next dev",
    "build": "pnpm vercel-build",
    "start": "next start",
    "lint": "next lint",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "vercel-build": "prisma generate && next build",
    "postbuild": "sentry-cli sourcemaps upload .next --url-prefix '~/_next' --rewrite || true"
  },
  "devDependencies": {
    "@sentry/cli": "^2.55.0",
    "@sentry/nextjs": "^10.16.0",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "prisma": "^5.21.1",
    "typescript": "^5"
  }
}
